- 重构Knowledge页面UI

1. 页面布局重构，分为两部分，左侧为文档列表（文档名称、状态、可见性、操作），右侧为列表选中文档的Metadata
2. 新增文档通过Drawer的形式在右侧弹出
3. 已有文档的“上传新版本”操作更名为“更新”，操作复用2的Drawer形式（逻辑上先删后增，用户感知上为更新）
4. 优化“更新”的性能，避免孤立分块残留等问题

- 导航栏重构：

1. 导航栏标签的间距减小，使其更加紧凑
2. Memory 标签默认放在 Trace 下方

- 已知Bug:

1. 更新Knowledge文档时，文档类型发生变化时无法正常更新（页面出现假死），如：JSON->JS，MarkDown->CSV等，测试时需要尽可能覆盖多的后缀文件（不同类型首次上传，同类型更新，不同类型更新等）
2. Session ID "23cc01e7-3479-4012-ba39-a05aec614131"、“1dbdb90c-9c0c-4673-857e-0a5d0ce7ab6a” 、“5375db29-613c-4edf-bdeb-312d3835c151”无法在 Trace 中查看 Runs 详情（内容缺失），但在 Chat 历史记录中有具体内容
