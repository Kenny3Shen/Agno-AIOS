# Changelog

本项目遵循语义化版本；发布日期以发布提交和 Git tag 为准。

## [1.0.0] - 2026-07-25

### Added

- 产品角色收口为 `admin` 与 `user`，并提供历史角色归并迁移与克隆库演练命令。
- 账户授权版本（`auth_version`）：角色、管理员状态或密码变化会立即撤销旧 JWT。
- 版本解析回归测试：API/OpenAPI 从 `T.A.I.S` 已安装发行包元数据读取版本。
- 发布核对文档，覆盖构建、测试、迁移演练、回滚记录与 tag 前检查。
- GitHub Actions `Quality` 工作流，持续执行后端静态检查/测试/构建与前端检查/E2E。

### Changed

- Dashboard Overview 遇到过期或不完整的 `agno_traces` 反射元数据时会串行刷新反射；若仍不兼容则安全回退到有界采样路径。
- 前后端版本与环境变量示例统一为 `1.0.0`。

### Security

- 审计日志仅向管理员开放；普通用户的审批读取、处理和重试强制限定为本人资源。
- 系统保留最后一名活跃管理员保护，并禁止管理员修改自己的访问级别。

## [0.6.0]

1.0.0 之前的开发记录保留在 [TODOs.md](./TODOs.md) 与 Git 历史中。
